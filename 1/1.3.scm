
(define (sqrt x) (* x x))

問題1.10まで


; (define (bigger x y)
;     (cond ((>= x y) x)
;           (else y))
;     )
;
; (display (bigger (bigger 3 4) 5))





; (define (bigger_two x y z)
;      (cond
;          (and (>= x y) (>= y z) x y)
;      )
; )
;
;
; 1  2 3
;


;(cond (and (>= x y) (>= y z)) (x y) 3) )


; (display (sqrt 3))
;

;(display (bigger_two 3 4 5))




;
; #gosh> (define (bigger_sqrt x y z)
