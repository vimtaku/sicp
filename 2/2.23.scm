


(display (+ 1 2))

; (define (for-each f li)
;  (if (null? (cdr li))
;   (f (car li))
;   (for-each f (cdr )
;
; )
;
; (for-each (lambda (x) (newline) (display x))
;           (list 57 321 88))
; 57
; 321
; 88
